# Arka Acres - Environment Variables Example
# Copy this file to .env.local for local development
# For production (Vercel), add these as environment variables in the Vercel dashboard

# ============================================
# REQUIRED - Database
# ============================================
# For local development (SQLite):
DATABASE_URL="file:./dev.db"

# For production (PostgreSQL on Vercel):
# DATABASE_URL="postgresql://user:password@host:5432/dbname?sslmode=require"
# Vercel provides a DATABASE_URL automatically if you use Vercel Postgres

# ============================================
# REQUIRED - Admin Authentication
# ============================================
# Admin email (used for password reset)
ADMIN_EMAIL="your-admin@example.com"

# Admin password (initial password, can be changed via reset flow)
ADMIN_PASSWORD="your-secure-password-here"

# ============================================
# REQUIRED - Site URLs (Production)
# ============================================
# Your production domain (e.g., https://arkaacres.com)
# Used for password reset links and email templates
NEXT_PUBLIC_SITE_URL="https://yourdomain.com"
NEXT_PUBLIC_APP_URL="https://yourdomain.com"

# ============================================
# Email Provider (Choose ONE: Resend OR SMTP)
# ============================================

# Option 1: Resend (Recommended for production)
RESEND_API_KEY="re_xxxxxxxxxxxxx"
EMAIL_FROM="Arka Acres <bookings@yourdomain.com>"
# Note: In production, EMAIL_FROM must be a verified domain in Resend

# Option 2: SMTP (Gmail/Google Workspace fallback)
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT="587"
# SMTP_SECURE="false"
# SMTP_USER="your-email@gmail.com"
# SMTP_PASS="your-app-password"  # Use Google App Password, not regular password

# ============================================
# SMS Provider (Optional - Twilio)
# ============================================
# TWILIO_ACCOUNT_SID="ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
# TWILIO_AUTH_TOKEN="your-auth-token"
# TWILIO_FROM_NUMBER="+1234567890"  # Your Twilio phone number
# SMS_TO_NUMBER="+14695369020"  # Where to send booking notifications

# ============================================
# Owner Contact (Defaults provided)
# ============================================
OWNER_EMAIL="arkaacres@gmail.com"
OWNER_PHONE="+14695369020"

# ============================================
# Optional Features
# ============================================
# Enable SMS notifications for guests (default: false)
# ENABLE_GUEST_SMS="false"

# Node environment (automatically set by Vercel in production)
# NODE_ENV="production"
